meta {
  name: postRol
  type: http
  seq: 2
}

post {
  url: {{base_url}}/rol
  body: json
  auth: none
}

body:json {
  {
    "nom_rol": "otro"
  }
}

script:pre-request {
  const { randRole } = require('@ngneat/falso')
  
  const randomRole = randRole()
  req.setBody({
    nom_rol: randomRole,
  });
  
}
